[package]
name = "remote-agent"
version = "0.6.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
thrift = "0.17"
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
ordered-float = "4"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
